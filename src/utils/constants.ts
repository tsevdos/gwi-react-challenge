export const endpoints = {
  root: "https://api.thecatapi.com/v1/",
  search: "images/search",
  images: "images",
} as const;

export const paths = {
  home: "/",
  cats: "/cats",
} as const;

// export const queryKeys = {
//   search: "search",
// } as const;
